services:
  # 🌐 Web service
  - type: web
    name: dobby-news-web
    env: python
    plan: free # có thể đổi sang starter/standard nếu cần CPU/RAM cao hơn
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python web_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1

  # ⚙️ Background worker
  - type: worker
    name: dobby-news-worker
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python news_runner.py --loop
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
